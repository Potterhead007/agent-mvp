# Secrets — never commit
.env
.env.plain
.env.enc
*.pem
*.key

# OpenClaw runtime (entire directory is generated at runtime)
.openclaw/

# Docker volumes
docker-volumes/

# OS
.DS_Store
Thumbs.db

# Rust build artifacts
target/

# Node (if local dev)
node_modules/
