# =============================================================================
# Agent MVP — Environment Variables
# =============================================================================
# Copy this file to .env.plain, fill in values, then run:
#   bash scripts/encrypt-env.sh
#
# NEVER commit .env.plain or .env — only this template.
# =============================================================================

# --- xAI Grok API ---
XAI_API_KEY=              # From https://console.x.ai — starts with "xai-"

# --- Telegram ---
TELEGRAM_BOT_TOKEN=       # From @BotFather — format: 123456789:ABCdef...

# --- Gateway ---
GATEWAY_TOKEN=            # Random 64-char hex: openssl rand -hex 32
GATEWAY_PORT=18790

# --- PostgreSQL ---
POSTGRES_PASSWORD=        # Random: openssl rand -hex 16
POSTGRES_USER=openclaw
POSTGRES_DB=openclaw
POSTGRES_PORT=5433

# --- Redis ---
REDIS_PASSWORD=           # Random: openssl rand -hex 16
REDIS_PORT=6380

# --- Security ---
OPENCLAW_DISABLE_BONJOUR=1
NODE_ENV=production
